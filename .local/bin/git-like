#!/usr/bin/env bash
#
# Reset the current branch to the exact state of a remote one.
#
# Usage:
#   git like upstream/master

IFS=/ read -a args <<< "$1"

remote=${args[0]:-origin}
branch=${args[1]:-$(git branch --show-current)}

git fetch "$remote"
git reset --hard "$remote/$branch"
